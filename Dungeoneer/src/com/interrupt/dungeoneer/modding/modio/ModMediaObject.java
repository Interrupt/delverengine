package com.interrupt.dungeoneer.modding.modio;

import com.badlogic.gdx.utils.Array;

public class ModMediaObject {
    /** Array of YouTube links. */
    public Array<String> youtube;

    /** Array of SketchFab links. */
    public Array<String> sketchfab;

    /** Array of image objects (a gallery). */
    public Array<ImageObject> images;
}
